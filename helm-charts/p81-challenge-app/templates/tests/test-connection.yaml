apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "p81-challenge-app.fullname" . }}-test-connection"
  labels:
    {{- include "p81-challenge-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "p81-challenge-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
