apiVersion: v1
kind: Service
metadata:
  name: {{ include "p81-challenge-app.fullname" . }}
  labels:
    {{- include "p81-challenge-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "p81-challenge-app.selectorLabels" . | nindent 4 }}
